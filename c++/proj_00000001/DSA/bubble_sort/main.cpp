#include <iostream>
#include <vector>

void bubbleSort (std::vector<int> &map) {
    for (int i=0;i<map.size()-1;i++) {
        for (int j=0;j<map.size();j++) {

        }
    }
};

int main () {
    std::vector<int> map = {1, 5, 2, 6, 3, 6, 2, 1, 6, 7, 5};
    bubbleSort(map);

    for (int i=0;i<map.size();i++) {

    }

    return 0;
}

