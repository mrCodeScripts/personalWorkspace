import { getUser, runTask, createSlug, logError, greetings, add, greet } from "./functions-return-types";
import { getFirstITEM, getFirstItem, getFirstITem, fetchData, a_user, a_product, datatable, aUserSomewhere } from "./generics";
// import { PartialOfUser, ReadonlyUser, USER_COLLECTIONS_2, UserGetter } from "./mapped-types";
import { user, firstObjectSample } from "./interfaces-vs-type-aliases";
import { processInput, handleError, makeSound, isApiUser } from "./type-narrowing-guards";
import { TheProduct, config, nameCollections, shopCollections, users, scores, names } from "./arrays-objects";
